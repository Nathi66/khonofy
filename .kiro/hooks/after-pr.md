# Hook: After PR

## Steps

1. Confirm PR URL and CI status (if CI exists).
2. Request reviews per `workflows/code-review.md`.
3. Address checklist items from `constitution/review-checklist.md`.
4. On merge, update `memory/release-history.md` only for release PRs.
