# Hook: After Commit

## Steps

1. Confirm commit hash and message locally (`git log -1`).
2. If commit closes a workflow step, note status for Loop Engineer.
3. Do not push unless requested by the user or release workflow.
